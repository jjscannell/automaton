# Discord Bot Configuration
DISCORD_BOT_TOKEN=

# Claude Code Authentication (optional if using Pro subscription)
ANTHROPIC_API_KEY=

# SSH Credentials for Claude Runner Container
# These create a user INSIDE the Docker container (not your local credentials)
SSH_USERNAME=automaton
SSH_PASSWORD=automaton

# ngrok (optional - only for external webhooks)
NGROK_AUTHTOKEN=
